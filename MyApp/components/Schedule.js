import React from 'react';
import { View, Text, StyleSheet, TouchableOpacity } from 'react-native';

const Schedule = (props) => {
  return (
    <Text>Putain ca marche</Text>
  )
}

export default Schedule;